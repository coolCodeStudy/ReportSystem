Whenever you are asked to commit or push code, you MUST ALWAYS run the `/test_all` workflow first.

If the `/test_all` workflow fails or tests do not pass, you are absolutely PROHIBITED from executing `git push` under any circumstances. Instead, you must pause and help the user fix the failing tests.
